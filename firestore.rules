/**
 * @fileoverview Firestore Security Rules for CheckersVerse.
 *
 * DEVELOPMENT MODE: These rules are configured to allow any authenticated user
 * to read and write to any document. This is for development purposes only.
 * For production, you MUST implement stricter, more granular security rules
 * to protect user data.
 */
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
